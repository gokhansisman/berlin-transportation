import React from "react";

import InformationBoard from "./InformationBoard";

function Departures({ information }) {
    return (
        <InformationBoard information={information} />
    );
}
export default Departures;
